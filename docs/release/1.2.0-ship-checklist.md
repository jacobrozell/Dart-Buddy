# Dart Buddy 1.2.0 — Ship checklist (draft)

**Status:** Draft branch cut from `dev` — not RC-ready until engineering gates below are green.

| | |
|--|--|
| **Version** | `1.2.0` |
| **Branch** | `release/1.2.0` |
| **Scope** | **1.1 gameplay** (7 modes) + **Training Partner** + **player export** + **German UI** (`en` + `de` in app bundle) |
| **Not in 1.2** | Modes tab, es/nl/fr/zh/it in **store** bundle (stay in repo on `dev`), App Intents in store builds |
| **Train doc** | [`ongoing-release-plan.md`](ongoing-release-plan.md) § 1.2 |
| **Surface** | [`ProductSurface.swift`](../../Support/Release/ProductSurface.swift) — `smart1_2` |
| **Store copy** | [`1.2.0-app-store-copy.md`](1.2.0-app-store-copy.md) |

**Last updated:** 2026-06-28

---

## Record (fill before RC)

| Field | Value |
|-------|-------|
| RC version | `1.2.0` |
| Build number | |
| Git commit / branch | `release/1.2.0` @ |
| Tester | |
| Date range | |
| Physical **iPhone** (model + iOS) | **Required** |
| TestFlight build # | |
| App Store Connect app ID | `6775713346` |

---

## Exit criteria (RC Go)

- [ ] **P0** device QA on physical iPhone (TestFlight or Release archive, **no launch args**)
- [ ] `ProductSurface.smart1_2` — seven gameplay modes + Training Partner + export reachable
- [ ] **1.1 → 1.2 upgrade** smoke on device (existing match data intact)
- [ ] **German UI** smoke — device language **Deutsch**: tab bar, Play setup, one party mode, Players, Settings
- [ ] **English UI** regression — device language **English** unchanged from 1.1 behavior
- [ ] Release archive contains **only** `en.lproj` + `de.lproj` (verify `.app` bundle)
- [ ] `GermanLocalizationSmokeUITests` green on RC commit
- [ ] CI green: `DartBuddyCI` + release UI lean suite on RC commit
- [ ] Privacy policy + App Store labels updated for export feature
- [ ] No P0 defects in device matrix

---

## §1 Engineering (P0)

- [ ] `MARKETING_VERSION` = `1.2.0` in [`project.yml`](../../project.yml)
- [ ] `CURRENT_PROJECT_VERSION` bumped for RC iteration
- [ ] `project.yml` bundles **en + de only** — see comment block in app target sources
- [ ] `CFBundleLocalizations`: `en`, `de` only
- [ ] No `DART_BUDDY_INTERNAL_BUILD` on Release (store branch)
- [ ] `ProductSurfaceTests` pass (smart allowlist + training/export + `["en","de"]` locales)
- [ ] `python3 Scripts/l10n.py audit` — no de key gaps vs `en`
- [ ] Training Partner flow — create/link after eligibility, setup roster, device match
- [ ] Player export (DBPE) — export + re-import smoke on device
- [ ] Reconcile with **`master`** (1.1.0 tag) before first RC if branch diverged

---

## §2 Product surface (P0)

*Release build, no launch args.*

### Gameplay (same seven as 1.1)

- [ ] X01, Cricket, Baseball, Killer, Shanghai, Raid, Around the Clock reachable from Play setup
- [ ] Modes tab **hidden**; other catalog modes **not** reachable

### 1.2 additions

- [ ] Training Partner visible in Players / setup when eligible
- [ ] Player export available in Settings or Players (per spec)

### German localization

- [ ] System language **Deutsch** → UI strings from `de.lproj` (not English fallback)
- [ ] Party mode names + rules sheets localized for shipped seven modes
- [ ] Training Partner + export strings localized in `de`
- [ ] Device set to **Español / Nederlands / Français** → **English** UI (locales not in store bundle)

---

## §3 App Store (after RC Go)

- [ ] English listing updated (Training Partner, export, German support called out in What's New)
- [ ] **German (Deutsch)** localized listing — subtitle, description, keywords ([`1.2.0-app-store-copy.md`](1.2.0-app-store-copy.md))
- [ ] German marketing screenshots (device language Deutsch) for App Store Connect **de** locale
- [ ] Submit build · tag `1.2.0` on **`master`** after approval · merge `release/1.2.0` → `master` → back-merge to `dev`

---

## Open before RC

1. Reconcile `release/1.2.0` with shipped **1.1.0** on `master` (SchemaV2, release highlights, analytics dimensions).
2. Define UI lean test target for 1.2 (`Smart1_2SmokeUITests` or extend party lean suite + German smoke in CI).
3. Finalize German App Store copy in [`1.2.0-app-store-copy.md`](1.2.0-app-store-copy.md).
